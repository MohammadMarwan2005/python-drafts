import numpy as np

print("Hello")
a = np.array([1, 2, 3])

# python3 -m venv .venv
# source .venv/bin/activate
# then you can easily install packages using pip, for example:
# pip install numpy
# pip install pandas

b = np.array([4, 5, 6])
c = a + b
print(c)
